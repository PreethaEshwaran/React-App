import { createContext } from 'react';
export const FormContext = createContext(null);
export const AddressContext = createContext(null);
export const DriverAddressContext = createContext(null);
export const HideAddressContext = createContext(null);
export const DriverFindAddressContext = createContext(null);


